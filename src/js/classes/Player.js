export default class Player{
    constructor(x=0, y=0, pOL=0, ){
        this.x = x;
        this.y = y;
        this.pointsOfLive = pOL;
        this.id = idPlayer 
    }

    draw(){
        console.log(this.id);
    }

}